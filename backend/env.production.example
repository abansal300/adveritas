# Production Environment Variables for Backend

# Database (Railway auto-provides this)
DATABASE_URL=postgresql://user:password@host:port/database

# Redis (Railway auto-provides this)
REDIS_URL=redis://host:port

# LLM Configuration - Choose ONE option:

## Option 1: Local Model (FREE but slower, good for demos)
USE_BEDROCK=false
VERDICT_MODEL=gpt2-medium

## Option 2: AWS Bedrock (PAID but better quality)
# USE_BEDROCK=true
# BEDROCK_MODEL_ID=meta.llama3-2-3b-instruct-v1:0
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-key-here
# AWS_SECRET_ACCESS_KEY=your-secret-here

# Embeddings Model
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Model Parameters
CLAIM_MIN_SCORE=0.35
VERDICT_TOPK=5

# CORS Origins (Add your Vercel domain)
CORS_ORIGINS=["http://localhost:3000","https://your-app.vercel.app"]

# Optional: MinIO Storage (for large files)
# MINIO_ENDPOINT=your-minio-endpoint
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=adveritas
