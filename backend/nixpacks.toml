[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -e ."]

[phases.build]
cmds = ["python -m nltk.downloader punkt stopwords"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
